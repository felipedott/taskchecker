<%= render 'shared/sidebar' %>
<%= render 'shared/navbar' %>

<div class="main">
    <div class="container mt-5">
      <div class="row">
        <div class="col-12 col-md-8 col-sm-6 ">
          <h1>Today's Tasks</h1>
            <div class="cards">
              <%= render 'tasks/tasks', tasks: @tasks %>
            </div>



        <hr class="my-5">

        <h1>Upcoming Meetings</h1>

          <div class="cards">
              <%= render 'events/events', event: @events %>
          </div>


      </div>
    </div>
  </div>
</div>
